name: Development CI

on:
  push:
    branches:
      - development   # ✅ Only triggers on this branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run your steps here
        run: echo "Running on development branch!"
